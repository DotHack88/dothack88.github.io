{
  "name": "BonsaiPhoto",
  "type": "object",
  "properties": {
    "bonsai_id": {
      "type": "string",
      "title": "Bonsai"
    },
    "photo_url": {
      "type": "string",
      "title": "URL foto"
    },
    "date": {
      "type": "string",
      "format": "date",
      "title": "Data"
    },
    "caption": {
      "type": "string",
      "title": "Didascalia"
    }
  },
  "required": [
    "bonsai_id",
    "photo_url",
    "date"
  ],
  "rls": {
    "create": {
      "created_by": "{{user.email}}"
    },
    "read": {
      "created_by": "{{user.email}}"
    },
    "update": {
      "created_by": "{{user.email}}"
    },
    "delete": {
      "created_by": "{{user.email}}"
    }
  }
}